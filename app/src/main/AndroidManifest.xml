<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.keyurv.prowallpaperandbackground">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SET_WALLPAPER_HINTS" />
    <uses-permission android:name="android.permission.SET_WALLPAPER" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProWallpaperAndBackground">
        <activity
            android:name=".FullScreenActivity"
            android:theme="@style/Theme.ApplyWallpaper" />
        <activity
            android:name=".SearchWallpaper"
            android:theme="@style/Theme.FullScreenMainActivity" />
        <activity
            android:name=".CategoriesWallpaper"
            android:theme="@style/Theme.FullScreenMainActivity" />
        <activity
            android:name=".RecentWallpaper"
            android:theme="@style/Theme.FullScreenMainActivity" />
        <activity
            android:name=".PopularWallpaper"
            android:theme="@style/Theme.FullScreenMainActivity" />
        <activity
            android:name=".WallpaperList"
            android:theme="@style/Theme.FullScreenMainActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.FullScreenMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>